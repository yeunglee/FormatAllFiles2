<Window x:Class="FormatAllFiles2.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Format All Files"
        Height="160" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow"
        Closing="Window_Closing">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Progress message -->
        <TextBlock Grid.Row="0" x:Name="MessageText"
                   Text="Formatting files..."
                   FontWeight="SemiBold" FontSize="13"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"/>

        <!-- Progress bar -->
        <ProgressBar Grid.Row="1" x:Name="FileProgressBar"
                     Minimum="0" Maximum="1" Height="24"
                     Margin="0,0,0,8"/>

        <!-- Current file detail -->
        <TextBlock Grid.Row="2" x:Name="DetailText"
                   Text="Starting..."
                   FontSize="11"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   TextTrimming="CharacterEllipsis"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,4"/>
    </Grid>
</Window>
